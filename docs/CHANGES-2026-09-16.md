# Presentation update — 2026-09-16

This follow-up records the requested changes to Junwen'Log. The original
implementation and migration documents remain historical records of the first
deployment.

## Requested changes

1. Remove “Every entry in the notebook, in reverse chronological order.” from
   the archive page.
2. Change tag text and borders from blue to Byzantine purple.
3. Remove the published article “实验室主机连接手册” and its bundled images.
4. Use the typography and compact navigation of
   [Lilian Weng's referenced article](https://lilianweng.github.io/posts/2026-07-04-harness/)
   as the visual reference; reduce the date/word-count metadata and give the
   article more room.
5. Use a centered, PaperMod-style table of contents that starts collapsed and
   expands when clicked.
6. Set the primary navigation to **Posts · Archive · Gallery · Tags**, followed
   by the appearance toggle and search.
7. Use article cover images as large, subtle backgrounds, respecting each
   image's proportions.

## Content and navigation changes

- The archive heading is now **Archive**, and its introductory sentence has
  been removed. Entries remain grouped by year and sorted newest first.
- Primary navigation maps **Posts** to `/posts/`, **Archive** to `/archives/`,
  **Gallery** to `/travel/`, and **Tags** to `/tags/`. **About** remains accessible
  in the footer. Reading notes remain available through the archive and tags.
- The lab-server guide page bundle was removed, including its eight images.
  Its former dated URL and legacy alias are intentionally no longer published;
  the original files remain recoverable from Git history.
- The WSL Ubuntu setup and Weibo user crawler articles' manually written
  tables of contents were removed so they do not duplicate the new
  collapsible table of contents. Their opening headings and remaining
  article text are preserved.

## Presentation changes

- System sans-serif body type at 16px with a 1.6 line height; 720px centered article column, 36px desktop titles, 60px navigation, and 14px metadata (12px on phones).
- Byzantine purple `#702963` for tag borders and text; dark mode uses `#d79acb` for legibility. Both article badges and the Tags index use these colors.
- A single native `details` table of contents above the article, initially closed. Its summary is centered; its expanded list retains readable indentation.
- Covers become responsive, proportion-preserving backgrounds with 12% opacity in light mode and 10% in dark mode. They add no height to the reading flow and fade towards the bottom.
- Local production checks pass for 52 HTML pages, including MathML, gallery assets, search, RSS and draft exclusion. Desktop, 390px mobile, light/dark appearances, and menu/TOC interactions were checked in the browser. See [VALIDATION.md](VALIDATION.md).
