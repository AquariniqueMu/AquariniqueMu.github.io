#!/bin/sh
# Resolve the repository from this script, so Finder launchers work from any cwd.
SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
if [ -x "$HOME/.local/bin/python3" ]; then
  exec "$HOME/.local/bin/python3" "$SCRIPT_DIR/blog.py" "$@"
fi
exec python3 "$SCRIPT_DIR/blog.py" "$@"
